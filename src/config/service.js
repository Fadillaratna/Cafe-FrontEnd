const BASE_URL = 'http://localhost:7000';

const services = {
  IMAGE: `${BASE_URL}/image`,
  USER: `${BASE_URL}/api/user`,
  TABLE: `${BASE_URL}/api/table`,
  MENU: `${BASE_URL}/api/menu`,
  TRANSACTION: `${BASE_URL}/api/transaction`
}

export default services;